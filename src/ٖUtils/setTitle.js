export function setCurrencyTitle(name="", symbol="") {
    return document.title = `${name} (${symbol}) | CoinCap.io`
}

export function setTitle(name="") {
    return document.title = `${name} | CoinCap.io`
}